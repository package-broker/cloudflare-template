{
  "name": "package-broker-deployment",
  "version": "1.0.0",
  "description": "PACKAGE.broker deployment to Cloudflare Workers",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@package-broker/main": "^0.17.4",
    "@package-broker/ui": "^0.17.4",
    "@package-broker/cloudflare": "^0.17.4"
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  }
}
